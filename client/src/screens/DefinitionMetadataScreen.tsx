import React from 'react'

const DefinitionMetadataScreen = () => (
  <div>
    <h1>DefinitionMetadataScreen</h1>
    <p>
      /definition/metadata
      - title
      - state
      - county name, county id
      - date - date picker, future only.
      - seal: select menu of built ins + custom which displays textarea for SVG
      - declarationMarkdown
      - instructionsMarkdown
    </p>
  </div>
)

export default DefinitionMetadataScreen
